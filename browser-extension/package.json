{
  "name": "@theseus/anti-conspirarcy-theorem-extension",
  "version": "0.2.0",
  "description": "Manifest V3 browser extension for ACC page analysis.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "eslint src tests --ext .js",
    "test": "node --test tests/*.test.js",
    "package": "npm run build && mkdir -p release && rm -f release/anti-conspirarcy-theorem-extension.zip && zip -r release/anti-conspirarcy-theorem-extension.zip dist/ manifest.json assets/ -x '*.map'"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "0.2.82"
  },
  "devDependencies": {
    "esbuild": "0.25.4",
    "eslint": "9.25.1"
  }
}
